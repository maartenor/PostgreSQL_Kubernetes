apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: postgres
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.179.206
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: postgres

# apiVersion: v1
# kind: Service
# metadata:
#   name: postgres
#   labels:
#     app: postgres
# spec:
#   type: NodePort
#   ports:
#     - port: 5432
#       nodePort: 30001 
#       targetPort: 5432
#   selector:
#     app: postgres